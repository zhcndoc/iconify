## Iconify for React 5.0.0 {#react-500}

适用于 React 的 Iconify 图标组件的新主版本已发布。

变更：
- 使用现代 React Hooks 重写。
- 支持 Next.js（作为纯客户端组件）。
- 软件包默认使用 ES 模块，同时也支持 CommonJS。
- 新增 `[prop]ssr` 属性，与近期 Vue 组件的更新类似。

请参阅[组件文档](/docs/icon-components/react/index.md)。
