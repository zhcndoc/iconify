该函数包含以下参数：

- `[prop]icon`，`[type]IconifyIcon`。图标数据。
- `[prop]customisations`。可选的自定义配置对象。

该函数返回包含图标数据的 `[type]object`，若图标不可用则返回 `null`。

在使用此函数之前，你需要先获取图标数据。请参阅 `[func]getIcon()`、`[func]iconLoaded()` 和 `[func]loadIcon()` 函数。
