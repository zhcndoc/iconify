```yaml
types:
  SVG: './svg/index.md'
  IconSet: './icon-set/index.md'
```

在 Iconify Tools 中，所有用于解析图标的函数都基于两个主要类：

- `[type]SVG` 代表单个图标。当需要操作图标的元素时，会使用此类。
- `[type]IconSet` 代表一个图标集，提供了易于使用的函数来管理图标。
