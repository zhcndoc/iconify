```yaml
title: 导入 SVG
types:
  SVG: '../svg/index.md'
  IconSet: '../icon-set/index.md'
```

# 导入 SVG

本教程是 [Iconify Tools](../index.md) 中 [导入函数文档](./index.md) 的一部分。

由于导入单个图标属于多余操作，因此没有提供专门的函数。您只需读取内容并创建一个新的 `[type]SVG` 实例即可：

## 示例

```yaml
src: libraries/tools/import/svg.ts
title: 'example.ts'
```
